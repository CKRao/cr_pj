# cr_pj

This is a minimal Go project initialized for development.

To run locally:

1. Build: `go build` which produces an executable in the current directory.
2. Run: `./cr_pj` then open http://localhost:8080 to see the message.
